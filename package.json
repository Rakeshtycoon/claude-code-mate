{
  "name": "business-diary",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Business Diary — a daily journal, finance tracker, client book and task manager for small businesses.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:single": "vite build && node scripts/inline.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
